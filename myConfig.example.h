// If required, add a delay before starting, may be needed to match STARTUP_DELAY
// #define STARTUP_DELAY 5000

// If required, define ALIVE_DELAY to show an alive message at this interval
// #define ALIVE_DELAY 5000
